import Game.Metadata

/-- If our goal is in an ugly form, and there's an obviously equivalent but much prettier representation, we can write `show representation` to change the goal to be `representation`. -/
TacticDoc «show»
