import Game.Metadata

/-- If we're being asked to prove something obvious enough for even the Lean compiler to prove without any extra work, we can call the `trivial` tactic to have Lean determine whether the statement is obvious enough to instantly prove. -/
TacticDoc trivial
