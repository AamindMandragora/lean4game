import Game.Metadata

/-- `rw` stands for rewrite. Let `l` and `r` be the left and right hand sides of some `lemma`, then `rw [lemma]` replaces every instance of `l` in the goal with `r`. If the goal instead has `r`s that we want to replace with `l`, we can do `rw [← lemma]` (the arrow is typed using \l). We can also do `rw [lemma] at h` to replace every instance of `l` in the hypothesis `h` with `r`. Basically, if we have some assumption that `a = b`, `rw` will substitute an `a` for a `b` in the goal. There actually is a tactic called `Rewrite`, but the difference is that `rw` inherently attemps to use `rfl` at the end, which saves a line of proof and renders the former tactic obsolete. If `lemma`'s hypothesis includes a variable `a`, then instead of letting Lean try and choose the first valid `a` in the proof, we can specify it ourselves using `rw [lemma b]`, where `b` is the variable we wanted to match. If `lemma` has multiple variables, we can specify all of them sequentially using `rw [lemma a b c ...]`. Similarly, if we have multiple `lemma`s we want to replace, we can use `rw [lemma1, lemma2, ...]`. -/
TacticDoc rw
